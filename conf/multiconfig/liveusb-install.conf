DISTRO = "liveusb"

MACHINE = "udoo-bolt-live-usb"

PREFERRED_PROVIDER_virtual/kernel = "liveusb-install-linux"

#KERNEL_PACKAGE_NAME = "liveusb-install"
KERNEL_ARTIFACT_NAME = "liveusb-install-${MACHINE}"

TMPDIR .= "-${BB_CURRENT_MC}"
