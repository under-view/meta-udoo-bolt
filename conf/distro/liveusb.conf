require ./common.conf

WKS_FILES = "liveusb.wks"
WKS_SEARCH_PATH = "${BOLT_LAYERDIR}/recipes-liveusb/images"

PREFERRED_PROVIDER_virtual/kernel = "liveusb-console-linux"

EMMC_DEPLOY_IMAGE_DIR = "${TMPDIR}-emmc/deploy/images/udoo-bolt-emmc"
LIVEUSB_INSTALL_DEPLOY_IMAGE_DIR = "${TMPDIR}-liveusb-install/deploy/images/${MACHINE}"
